import React from 'react';

// FIX: This component is removed as per API key guidelines.
// The application must use process.env.API_KEY and not prompt the user.
const ApiKeyModal: React.FC = () => {
  return null;
};

export default ApiKeyModal;
